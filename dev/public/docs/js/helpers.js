// AM: checks if a given string exists in another one.
// function doesContain(key){return indexOf(url)!==-1 ? true : false; }

String.prototype.doesContain = function (key) {return this.indexOf(key)!==-1 ? true : false;}
  
  


